<Window x:Class="WpfAppAITest.Views.BusyWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             WindowStyle="None"
             ResizeMode="NoResize"
             Background="Transparent"
             AllowsTransparency="True"
             WindowStartupLocation="CenterScreen"
             ShowInTaskbar="False"
             Topmost="True"
             SizeToContent="WidthAndHeight">

    <Grid Background="Transparent">
        <Border Width="200" Height="110" Background="#80000000" CornerRadius="10">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="Refresh" Spin="True" Height="48" Width="48" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#00285a" Margin="5"></fa:ImageAwesome>
                <TextBlock Name="MessageBlock" MaxWidth="200" TextWrapping="Wrap" TextAlignment="Center"
                           Foreground="White" 
                           FontSize="16" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center" 
                           Margin="10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
